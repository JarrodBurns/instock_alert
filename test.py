
import tkinter as tk
from tkinter import *
## Main

# photo = PhotoImage(file="logo_smaller.gif")
# photo_label = Label(root, image=photo, bg="grey20")
# photo_label = photo_label.grid(row=0, column=0)

# photo2 = PhotoImage(file="bot.gif")
# photo_label2 = Label(root, image=photo2, bg="grey20")
# photo_label2 = photo_label2.grid(row=0, column=1)


# a = Label(root, text="SOme text here", bg="grey20", fg="white", font="none 12 bold")
# a.grid(row=1, column=0, sticky=W)

# textentry = Entry(root, width=20, bg="white")
# textentry.grid(row=2, column=0)
# root = tk.Tk()
# root.title("GPU Bot")

# canvas = tk.Canvas(root, height=700, width=700, bg="grey20")
# canvas.pack()

# frame = tk.Frame(root, bg="white")
# # frame.place(relwidth=0.8, relheight=0.8, relx=0.1, rely=0.1)
# frame.place(relwidth=0.9, relheight=0.9, relx=0.05, rely=0.05)
#

# #shows the gui
# root.mainloop()

CON = 0


CON / 2


for _ in range(1, 11):
    print("Hey")
